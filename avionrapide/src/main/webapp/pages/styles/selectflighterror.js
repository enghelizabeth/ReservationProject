/**
 * 
 */


function sufficientflightsselected() {
	/*var departCheckbox = document.getElementsByName("selectedDepartingFlightNumber[]");
	var onlyOneCheckDepart = false;
	var numOfCheckedBoxes = 0;
	var bool = false;
	console.log("Here in selectflight errors");
	document.getElementById('departFlightSelectedError').hidden = true;
	//document.getElementById('departingDateError').hidden = true;

	for (var idx = 0; i < departCheckbox.length; idx++) {
		if (departCheckbox[idx].checked) {
			if (numOfCheckedBoxes == 0) {
				numOfCheckedBoxes++;
				onlyOneCheckDepart = true;
			}
			else {
				document.getElementById('departFlightSelectedError').hidden = false;
				bool = false;
			}
		}
	}
	if (!onlyOneCheckDepart) {
		document.getElementById('departFlightSelectedError').hidden = false;
	}


	return bool;*/

	return false;
}